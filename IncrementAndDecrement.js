'use strict'
let x = 0;
let y = x++;
// Увеличивает аргумент на единицу и возвращает СТАРОЕ значение
console.log( y );
y = ++x;
// Увеличивает аргумент на единицу и возвращает НОВОЕ значение
console.log( y );
